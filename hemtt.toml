name = "S.O.G. Vanilla Main Menu"
prefix = "sogvanillamainmenu"
author = "SzwedzikPL"
mainprefix = "z"
files = [
    "mod.cpp",
    "README.md",
    "LICENSE"
]

modname = "{{name}}"
key_name = "{{prefix}}_{{version}}"
authority = "{{prefix}}_{{version}}-{{git \"id 8\"}}"

releasebuild = [
    "@zip sogvanillamainmenu_{{semver.major}}.{{semver.minor}}.{{semver.patch}}"
]

[header_exts]
version= "{{git \"id 8\"}}"
